import React from "react";

/**
 * Render a full-width banner image, with text and buttons on top.
 * @param props
 * @param {string} props.title
 * @param {string} props.description
 * @param {?{ text: string, url: string }[]} props.links
 * @param {?boolean} props.centered
 * @returns
 */
export default function Hero(/* { title, description, links = [], centered = false } */) {
  return <>Hero placeholder</>;
}

import React from "react";

/**
 * Display information about an event.
 * @param props
 * @param {string} props.title
 * @param {string} props.description
 * @param {string} props.place
 * @param {Date} props.time
 * @param {string} props.url
 */
export default function Event(/* { title, description, place, time, url } */) {
  return <>Event placeholder</>;
}
